/**
 * firebase.js this class is used as a header to create one single
 * firebase connection on any page, no js files below this will create the connection
 * This requires the firebase library to be loaded before this js can be used
  */ 

"use strict";
var fireBaseRef = new Firebase('torrid-fire-6240.firebaseIO.com');
